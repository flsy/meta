name: Build and Test
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: yarn
    - run: yarn prettier
    - run: yarn lint
    - run: yarn build
    - run: yarn jest
